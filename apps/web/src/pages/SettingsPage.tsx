import { PageHeader } from '../components/PageHeader'

export function SettingsPage() {
  return (
    <section>
      <PageHeader title="Settings" subtitle="Worker and storage settings are configured by wrangler and environment bindings." />
    </section>
  )
}
